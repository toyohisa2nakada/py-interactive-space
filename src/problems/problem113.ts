import type { Problem } from './types';

const problem: Problem = {
  id: 113,
  title: "Trueと判定されるように、空欄に数字を入力してください",
  type: 'fill-in',
  codeWithMarker: `print(§§ in [5, 10])
`,
  correctOutput: "True",
};

export default problem;
