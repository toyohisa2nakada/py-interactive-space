import type { Problem } from './types';

const problem: Problem = {
  id: 44,
  title: "-7 の絶対値を予想してください",
  type: 'predict-output',
  code: `print(abs(-7))
`,
  correctOutput: "7",
};

export default problem;
