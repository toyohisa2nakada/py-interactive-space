import type { Problem } from './types';

const problem: Problem = {
  id: 4,
  title: "文字列の出力",
  type: 'predict-output',
  code: `name = "Python"
print(name)
`,
  correctOutput: "Python",
};

export default problem;
