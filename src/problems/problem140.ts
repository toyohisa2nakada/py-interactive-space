import type { Problem } from './types';

const problem: Problem = {
  id: 140,
  title: "辞書型データ(item)から「price」の値を出力するプログラムを完成させてください",
  type: 'fill-in',
  codeWithMarker: `item = {"name": "apple", "price": 150}
print(§§§§§§)
`,
  correctOutput: "150",
  requiredTokens: ['item','price'],
};

export default problem;
