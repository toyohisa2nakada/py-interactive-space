import type { Problem } from './types';

const problem: Problem = {
  id: 25,
  title: "sum関数",
  type: 'predict-output',
  code: `print(sum([1, 2, 3, 4, 5]))
`,
  correctOutput: "15",
};

export default problem;
