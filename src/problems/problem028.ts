import type { Problem } from './types';

const problem: Problem = {
  id: 28,
  title: "in演算子: リスト検索",
  type: 'predict-output',
  code: `print(3 in [1, 2, 3, 4, 5])
`,
  correctOutput: "True",
};

export default problem;
